Act as an expert Senior Front-End Developer and UI/UX Designer. I want you to build a web application for [ΠΕΡΙΓΡΑΨΤΕ ΕΔΩ ΤΟ ΘΕΜΑ ΤΗΣ ΕΦΑΡΜΟΓΗΣ ΣΑΣ].

Please follow the exact "Design System" and "Technical Stack" described below to replicate a specific high-quality look and feel.

### 1. Technical Stack (No Build Step)
- **Core:** HTML5, Vanilla JavaScript (ES6+), CSS.
- **Styling:** Tailwind CSS (load via CDN script).
- **Icons:** Heroicons (SVG strings inline).
- **Libraries (if data viz needed):** Chart.js (CDN) and Numeral.js (CDN) for formatting.
- **Architecture:** Client-side only. Logic in a single `scripts.js` or inside the HTML. No frameworks like React/Vue.

### 2. Design System & Aesthetics (The "Look")
The style must be "Modern Clean Dashboard":
- **Color Palette:**
  - Background: `bg-slate-50` (very light grey/white smoke).
  - Text: `text-slate-900` for headings, `text-slate-600` for body/labels.
  - Primary Brand: `indigo-600` (for primary buttons, active states, key highlights).
  - Accents: `emerald-500` for positive numbers, `red-500` for negative/deductions.
- **Card Styling (Crucial):**
  - All containers must use `bg-white`, `rounded-2xl` or `rounded-3xl`, `shadow-xl` (soft shadow), and a thin border `border-slate-100`.
- **Inputs:**
  - Large, airy inputs. Classes: `w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-4 outline-none focus:ring-2 focus:ring-indigo-500 transition-all font-bold text-slate-900`.
  - Labels: Uppercase tracking-wider text-xs or bold text-sm text-slate-700.
- **Buttons:**
  - Primary: `bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-xl shadow-lg shadow-indigo-200 transition-colors`.
  - Secondary: `border-2 border-slate-200 text-slate-600 hover:bg-slate-50 rounded-xl`.
- **Visual Polish:**
  - Use "Glow" effects for summary cards (e.g., a dark blue/indigo card with absolute positioned, blurred circles in the background for texture).
  - Use ample whitespace (padding `p-8` or `p-10`) inside cards.

### 3. Layout Structure
- **Navbar:** Sticky top, white, shadow-sm, containing the Logo and a badge for the current year/status.
- **Main Grid:** Use `max-w-7xl mx-auto`.
- **Dashboard Layout:** A responsive grid (`grid-cols-1 lg:grid-cols-12`).
  - Left/Top (approx 7 cols): Input forms and controls.
  - Right/Bottom (approx 5 cols): "Sticky" or prominent summary card (dark background) showing live results.
- **Charts:** Place charts in separate white cards with a clean legend.

### 4. Functionality Requirements
- **Real-Time:** Use `addEventListener('input', ...)` so calculations happen instantly as the user types. No "Submit" button unless necessary.
- **Input Handling:** Inputs should support local number formats (comma for decimals if Greek locale).
- **SPA Feel:** If multiple views are needed (e.g., "Dashboard" vs "Analysis"), use JavaScript to toggle CSS `hidden` classes on `div` containers instead of page reloads.

### Task
Create the code (HTML, CSS, JS) for [ΠΕΡΙΓΡΑΨΤΕ ΤΗ ΛΕΙΤΟΥΡΓΙΑ ΤΗΣ ΕΦΑΡΜΟΓΗΣ]. Ensure the UI matches the detailed specs above strictly.